# Drop the unused legacy adapter

The test depended on wall-clock ordering. It now uses a fake clock and passes deterministically.

Change #2 of 4 on branch `pr/20260811-115807-2-drop-the-unused-legacy-adapter`.
